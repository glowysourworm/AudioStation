<UserControl x:Class="AudioStation.Views.DialogViews.SingleTextFieldView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:propertyGrid="clr-namespace:AudioStation.Controls.PropertyGrid"
             Width="360">
    <Grid Margin="10">
        <propertyGrid:PropertyStringControl Style="{StaticResource PropertyGridUserControl}"
                                            LabelColumnWidth="160"
                                            LabelText="{Binding Name}"
                                            Value="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
    </Grid>
</UserControl>
