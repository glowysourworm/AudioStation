<UserControl x:Class="AudioStation.Views.LibraryImportView"
             x:Name="BaseControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xceed="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:importView="clr-namespace:AudioStation.Views.LibraryImportViews"
             xmlns:region="clr-namespace:SimpleWpf.IocFramework.RegionManagement;assembly=SimpleWpf.IocFramework"
             xmlns:fa5="http://schemas.fontawesome.com/icons/"
             xmlns:propertyGrid="clr-namespace:AudioStation.Controls.PropertyGrid"
             xmlns:coreModel="clr-namespace:AudioStation.Core.Model;assembly=AudioStation.Core"
             xmlns:simpleUI="clr-namespace:SimpleWpf.UI.Controls;assembly=SimpleWpf.UI">
    
    <Grid RowDefinitions="*, Auto">

        <!-- Import Files -->
        <Border BorderBrush="LightGray"
                BorderThickness="1"
                CornerRadius="3"
                Margin="5"
                Padding="0">
            <region:IocRegion Margin="0"
                              Padding="0"
                              region:IocRegionManager.RegionName="LibraryImporterControlRegion"
                              region:IocRegionManager.DefaultViewType="{x:Type importView:LibraryImportStagingView}" />
        </Border>

        <!-- Import "Wizard" -->
        <StackPanel Orientation="Horizontal" 
                    VerticalAlignment="Center"
                    HorizontalAlignment="Right"
                    Margin="5 0 5 5"
                    Grid.Row="1">

            <!-- Previous -->
            <Button x:Name="PreviousButton"
                    Margin="0"
                    Click="PreviousButton_Click"
                    IsEnabled="{Binding PreviousStepReady, ElementName=BaseControl}"
                    Style="{StaticResource UserControlButton}">
                <StackPanel Orientation="Horizontal">
                    <fa5:SvgAwesome Icon="Solid_ArrowLeft" Style="{StaticResource FontAwesomeIconDefault}" Margin="5" />
                    <TextBlock Text="Previous" Style="{StaticResource LabelTextBlock}" Margin="5" />
                </StackPanel>
            </Button>

            <!-- Next -->
            <Button x:Name="NextButton" 
                    Margin="5 0 0 0" 
                    Click="NextButton_Click"
                    IsEnabled="{Binding NextStepReady, ElementName=BaseControl}"
                    Style="{StaticResource UserControlButton}">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Next" Style="{StaticResource LabelTextBlock}" Margin="5" />
                    <fa5:SvgAwesome Icon="Solid_ArrowRight" Style="{StaticResource FontAwesomeIconDefault}" Margin="5" />                    
                </StackPanel>
            </Button>

        </StackPanel>

    </Grid>
</UserControl>
