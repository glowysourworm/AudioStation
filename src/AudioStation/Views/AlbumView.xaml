<UserControl x:Class="AudioStation.Views.AlbumView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
			 xmlns:fa5="http://schemas.fontawesome.com/icons/"    
             xmlns:local="clr-namespace:AudioStation.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Padding="10">
        <Grid>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            
            <Image Source="{Binding FileNameRef, Converter={StaticResource ArtworkFileConverter}}"
				   Height="200" Width="200" Grid.RowSpan="2"
				   VerticalAlignment="Top" />

            <!-- Album Banner -->
            <Border Background="AliceBlue" Grid.Column="1">
                <Grid Margin="5 0 0 0">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Artist}" HorizontalAlignment="Left" FontSize="16" Foreground="#555555" Margin="3 5" />
                        <fa5:SvgAwesome Icon="Solid_Circle" Width="3" Height="3" Foreground="#555555" Margin="10" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding Album}" HorizontalAlignment="Left" FontSize="16" Foreground="#555555" Margin="3 5" />
                    </StackPanel>


                    <!-- Album Year / Genre / Other Details... -->
                    <TextBlock Text="{Binding Year}" HorizontalAlignment="Left" FontSize="12" Foreground="Gray" Grid.Row="1" Margin="3" />
                    <TextBlock Text="{Binding Duration, StringFormat=HH:mm:ss}" HorizontalAlignment="Right" FontSize="12" Foreground="Gray" Grid.Row="1" Margin="3" />

                </Grid>
            </Border>

            <ListBox ItemsSource="{Binding Tracks}"
					 Grid.Column="1" Grid.Row="1"
					 Background="White">

                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border>
                            <Grid>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <TextBlock Text="{Binding Track}" Grid.Column="1" FontSize="12" Foreground="Gray" Margin="3" />
                                <TextBlock Text="{Binding Name}" Grid.Column="2" FontSize="12" Foreground="Gray" Margin="3" />
                                <TextBlock Text="{Binding Duration, StringFormat=HH:mm:ss}" Grid.Column="3" FontSize="12" Foreground="Gray" Margin="3" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>
    </Border>
</UserControl>
