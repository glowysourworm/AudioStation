<UserControl x:Class="AudioStation.Views.LibraryImportViews.LibraryImportFileDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:xceed="http://schemas.xceed.com/wpf/xaml/toolkit"
             xmlns:fa5="http://schemas.fontawesome.com/icons/"
             xmlns:propertyGrid="clr-namespace:AudioStation.Controls.PropertyGrid"
             xmlns:controls="clr-namespace:AudioStation.Controls"
             xmlns:importView="clr-namespace:AudioStation.Views.LibraryImportViews"
             xmlns:coreModel="clr-namespace:AudioStation.Core.Model;assembly=AudioStation.Core"
             xmlns:simpleUI="clr-namespace:SimpleWpf.UI.Controls;assembly=SimpleWpf.UI">
    
    <controls:NoSelectListBox
            Grid.Row="1"
            Background="White"
            BorderBrush="Gray" 
            BorderThickness="0"
            HorizontalContentAlignment="Stretch"
            ItemContainerStyle="{StaticResource NoHoverSelectItemContainerStyleWithAlteration}"
            AlternationCount="2"
            ScrollViewer.VerticalScrollBarVisibility="Auto"
            ScrollViewer.HorizontalScrollBarVisibility="Hidden">

        <propertyGrid:PropertyLabelControl LabelColumnWidth="140" LabelText="Creation"
                                               Value="{Binding CreationUtc}" ToolTip="{Binding FullPath}" />

        <propertyGrid:PropertyLabelControl LabelColumnWidth="140" LabelText="Last Write"
                                               Value="{Binding LastWriteUtc}" ToolTip="{Binding FullPath}" />

        <propertyGrid:PropertyLabelControl LabelColumnWidth="140" LabelText="Full Path"
                                               Value="{Binding FullPath}" ToolTip="{Binding FullPath}" />

        <propertyGrid:PropertyLabelControl LabelColumnWidth="140" LabelText="Migrated File"
                                               Value="{Binding FileMigrationFullPath, TargetNullValue='(Tag Issues)'}" ToolTip="{Binding FileMigrationFullPath}"
                                               Foreground="{Binding MinimumImportValid, Converter={StaticResource ValidationForegroundParameterConverter}, ConverterParameter={StaticResource LabelForeground}}" />

        <propertyGrid:PropertyLabelControl LabelColumnWidth="140" LabelText="Import Options" 
                                               Value="{Binding ImportOptions}" />

        <propertyGrid:PropertyLabelControl LabelColumnWidth="140" LabelText="AcoustID" 
                                                       Value="{Binding SelectedAcoustIDResult.Score, StringFormat=P2}"
                                                       Foreground="{Binding ImportOutput.AcoustIDSuccess, Converter={StaticResource ValidationForegroundParameterConverter}, ConverterParameter={StaticResource LabelForeground}}">
        </propertyGrid:PropertyLabelControl>

        <propertyGrid:PropertyLabelControl LabelColumnWidth="140" LabelText="Music Brainz" Grid.Row="1"
                                                       Value="{Binding SelectedMusicBrainzRecordingMatch.ShortString}"
                                                       ToolTip="{Binding SelectedMusicBrainzRecordingMatch}"
                                                       Foreground="{Binding ImportOutput.MusicBrainzRecordingMatchSuccess, Converter={StaticResource ValidationForegroundParameterConverter}, ConverterParameter={StaticResource LabelForeground}}">
        </propertyGrid:PropertyLabelControl>

    </controls:NoSelectListBox>
</UserControl>
