<Window x:Class="AudioStation.Windows.ImportOptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dialogView="clr-namespace:AudioStation.Views.DialogViews"
        Icon="/Resources/Images/icon.bmp"
        Title="Import Options">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <dialogView:LibraryLoaderImportOptionsView />

        <!-- Status Bar (Footer) -->
        <Border x:Name="StatusBorder" Grid.Row="1" Background="#EEEEEE">
            <TextBlock x:Name="StatusMessageTB" 
					   Text="Status Bar" 
					   FontSize="14" Margin="5" 
					   Foreground="#555555" VerticalAlignment="Center" />
        </Border>
    </Grid>
</Window>
